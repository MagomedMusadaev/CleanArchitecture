project
├── api
│   └── http
│       └── router.go      // Ответственный за маршрутизацию HTTP-запросов.
├── internal
│   ├── handlers
│   │   └── user_handler.go // Логика обработки запросов (создание, удаление, получение данных пользователя).
│   ├── repositories
│   │   └── in_memory_db.go  // Реализация мнимой базы данных, где хранятся данные.
│   │   └── user_repo.go     // Репозиторий для работы с пользователями (посредник между сервисом и данными).
│   ├── services
│   │   └── user_service.go  // Сервис для бизнес-логики (работает с репозиторием и реализует действия).
│   ├── models
│   │   └── user.go         // Модели данных (например, структура пользователя).
└── cmd
    └── main.go              // Основной файл для запуска приложения.
